# Exemplo de variáveis de ambiente necessárias.
# Crie um arquivo chamado .env (sem o .example) na raiz do seu projeto local
# e coloque seus valores reais lá. NÃO envie o arquivo .env para o GitHub!

DATABASE_URL="postgresql+psycopg2://USUARIO:SENHA@HOST_DO_BANCO:PORTA/NOME_DO_BANCO"

# Outras variáveis podem ser adicionadas aqui no futuro (ex: chaves de API, etc.)
# SECRET_KEY="sua_chave_secreta_aqui"
